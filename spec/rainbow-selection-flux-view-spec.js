'use babel';

import RainbowSelectionFluxView from '../lib/rainbow-selection-flux-view';

describe('RainbowSelectionFluxView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
